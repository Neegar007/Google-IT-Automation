#!/usr/bin/.venv python3
# run :
# ~$ python3 stdout.py > stdoutput.txt
# This will be printed into another file
# ----------------------------------------------------
# ~$ python3 stdout.py >> stdoutput.txt 
# This will append the line instead of overwriting it
print("A text to pour into STDOUT stream and port into another file with bash" \
"'>' sign.")